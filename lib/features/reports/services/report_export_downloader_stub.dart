Future<void> downloadTextFile({
  required String fileName,
  required String mimeType,
  required String content,
}) async {
  throw UnsupportedError(
    'Download de arquivo disponivel apenas na versao web nesta etapa.',
  );
}
